import { FC } from 'react'
import styles from '@/components/PageLoader/PageLoader.module.scss'

const PageLoaderLine: FC = () => <div id='loader-line' className={styles.decLine} />

export default PageLoaderLine